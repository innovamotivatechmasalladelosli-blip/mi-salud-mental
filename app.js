const $ = (selector, parent = document) => parent.querySelector(selector);
const $$ = (selector, parent = document) => [...parent.querySelectorAll(selector)];
const store = { get(key, fallback) { try { return JSON.parse(localStorage.getItem(key)) ?? fallback; } catch { return fallback; } }, set(key, value) { localStorage.setItem(key, JSON.stringify(value)); } };
const modalBackdrop = $('#modalBackdrop');
const modalContent = $('#modalContent');
let timer = null, phaseTimer = null, secondsLeft = 0, audio = null, audioMode = 'lluvia';
const questions = ['¿Qué necesitas darte permiso de sentir?', '¿Qué sería amable contigo en los próximos 10 minutos?', '¿Qué parte de tu día merece más espacio?', '¿Qué te gustaría recordar cuando termine este día?', '¿Qué sí está bajo tu control ahora?'];
const todayKey = () => new Date().toISOString().slice(0, 10);
const breathingModes = {
  calm: { name: 'Calmar la mente', pattern: [4, 6], labels: ['Inhala', 'Exhala'], hint: 'Para bajar revoluciones y soltar tensión.' },
  anxious: { name: 'Cuando hay ansiedad', pattern: [4, 2, 6], labels: ['Inhala', 'Pausa suave', 'Exhala'], hint: 'Sin forzar: deja que la exhalación sea un poco más larga.' },
  energy: { name: 'Despertar con energía', pattern: [4, 4], labels: ['Inhala', 'Exhala'], hint: 'Para activar el cuerpo con presencia.' },
  sleep: { name: 'Prepararte para dormir', pattern: [4, 8], labels: ['Inhala', 'Exhala'], hint: 'Lento y cómodo; si te mareas, vuelve a tu respiración natural.' }
};
const recommendations = {
  1: { label: 'Hoy necesitas suavidad', text: 'Empieza con una pausa de 60 segundos y vuelve a tu cuerpo.', mode: 'breathing', breath: 'calm' },
  2: { label: 'Vamos paso a paso', text: 'Una exhalación larga puede darte un poco de espacio.', mode: 'breathing', breath: 'anxious' },
  3: { label: 'Puedes escucharte sin resolverlo todo', text: 'Prueba anclaje 5–4–3–2–1 para volver al presente.', mode: 'grounding' },
  4: { label: 'Hay energía disponible', text: 'Muévete cinco minutos o regálate una práctica de conexión.', mode: 'movement' },
  5: { label: 'Qué bueno encontrarte aquí', text: 'Aprovecha este momento para agradecer o volver a tus valores.', mode: 'gratitude' }
};
function showToast(message) { const toast = $('#toast'); toast.textContent = message; toast.classList.add('show'); clearTimeout(showToast.timer); showToast.timer = setTimeout(() => toast.classList.remove('show'), 3200); }
function markToday(label = 'Práctica completada') { const progress = store.get('weekProgress', {}); progress[todayKey()] = true; store.set('weekProgress', progress); renderWeek(); refreshProgress(); showToast(`${label}. Lo pequeño también cuenta.`); }
function vibrate(pattern = 20) { if (store.get('vibration', true) && navigator.vibrate) navigator.vibrate(pattern); }
function stopAudio() { if (!audio) return; try { audio.master.gain.exponentialRampToValueAtTime(.0001, audio.ctx.currentTime + .18); setTimeout(() => { audio?.ctx.close(); audio = null; }, 220); } catch { audio = null; } }
function startAudio(mode = audioMode) { if (audio) return true; const AudioContext = window.AudioContext || window.webkitAudioContext; if (!AudioContext) { showToast('Tu navegador no admite sonido; continuaremos sin audio.'); return false; } const ctx = new AudioContext(); const master = ctx.createGain(); master.gain.value = .0001; master.connect(ctx.destination); const osc = ctx.createOscillator(); const filter = ctx.createBiquadFilter(); const presets = { lluvia: [174, 520], bosque: [220, 760], mar: [130, 360], blanco: [90, 1200] }; const [frequency, cutoff] = presets[mode] || presets.lluvia; osc.type = mode === 'blanco' ? 'triangle' : 'sine'; osc.frequency.value = frequency; filter.type = 'lowpass'; filter.frequency.value = cutoff; osc.connect(filter).connect(master); osc.start(); master.gain.exponentialRampToValueAtTime(.045, ctx.currentTime + .5); audio = { ctx, master, osc }; return true; }
function chime() { if (!audio) return; const o = audio.ctx.createOscillator(), g = audio.ctx.createGain(); o.type = 'sine'; o.frequency.value = 392; g.gain.setValueAtTime(.0001, audio.ctx.currentTime); g.gain.exponentialRampToValueAtTime(.035, audio.ctx.currentTime + .03); g.gain.exponentialRampToValueAtTime(.0001, audio.ctx.currentTime + .65); o.connect(g).connect(audio.master); o.start(); o.stop(audio.ctx.currentTime + .7); vibrate(18); }
function closeModal() { clearInterval(timer); clearInterval(phaseTimer); timer = phaseTimer = null; stopAudio(); modalBackdrop.hidden = true; document.body.style.overflow = ''; }
function audioControls() { return `<div class="audio-controls"><label>Ambiente <select id="audioMode"><option value="lluvia">Lluvia suave</option><option value="mar">Mar tranquilo</option><option value="bosque">Bosque</option><option value="blanco">Ruido blanco</option></select></label><button class="sound-toggle" id="soundToggle" aria-pressed="false">♩ Sonido apagado</button><button class="sound-toggle" id="vibrationToggle" aria-pressed="${store.get('vibration', true)}">${store.get('vibration', true) ? '⌁ Vibración activa' : '⌁ Vibración apagada'}</button></div>`; }
function breathingView() { return `<p class="eyebrow">Práctica sensorial · 2 min</p><h2 id="modalTitle">Respirar con la esfera</h2><p class="modal-intro">Elige cómo quieres acompañarte hoy. La esfera marca el ritmo; no necesitas hacerlo perfecto.</p><div class="breathing-modes" role="group" aria-label="Modo de respiración">${Object.entries(breathingModes).map(([key, mode], i) => `<button class="breathing-mode ${i === 0 ? 'selected' : ''}" data-breath-mode="${key}"><strong>${mode.name}</strong><small>${mode.pattern.join(' · ')} s</small></button>`).join('')}</div><div class="guided-breathing"><div class="breath-orb" id="breathOrb"><span id="breathPhase">Listo</span><small id="breathSeconds">—</small></div><div class="breath-ripples"></div></div><div class="timer-label" id="timerLabel">2 min · a tu propio ritmo</div><div class="timer-display" id="timerDisplay">02:00</div><div class="modal-actions"><button class="button button-primary" id="timerStart">Comenzar</button><button class="button button-dark" id="timerReset">Reiniciar</button></div>${audioControls()}<p class="mini-note" id="breathHint">${breathingModes.calm.hint}</p>`; }
const timerViews = { movement: ['Físico · 5 min', 'Movimiento amable', 'Activa tu cuerpo sin exigencia: camina en el lugar, haz círculos de hombros y vuelve a respirar.'], chairSquats: ['Físico · 6 min', 'Sentadillas a silla', 'Coloca una silla firme detrás de ti. Baja hasta rozarla y vuelve a subir, manteniendo las rodillas cómodas.'], walk: ['Físico · 8 min', 'Caminata consciente', 'En un lugar seguro, camina a un ritmo cómodo. Nota cómo apoyan tus pies y cómo se mueven tus brazos.'], mobility: ['Físico · 5 min', 'Movilidad de espalda', 'Haz círculos de hombros, rotación suave de torso y balanceo de cadera, sin rebotes.'], balance: ['Físico · 4 min', 'Equilibrio con apoyo', 'Ponte junto a una pared o silla firme. Eleva un pie unos segundos y cambia de lado.'], dance: ['Físico · 7 min', 'Baile de una canción', 'Elige una canción que te guste. Mueve hombros, brazos, cadera o pies; no hay pasos correctos.'] };
function timerView(type) { const [eyebrow, title, intro] = timerViews[type]; const minutes = ({ movement: 5, chairSquats: 6, walk: 8, mobility: 5, balance: 4, dance: 7 })[type]; return `<p class="eyebrow">${eyebrow}</p><h2 id="modalTitle">${title}</h2><p class="modal-intro">${intro}</p><div class="movement-visual"><div class="movement-dot"></div><span id="movementCue">Listo para moverte</span></div><div class="timer-label" id="timerLabel">Muévete sin dolor ni exigencia</div><div class="timer-display" id="timerDisplay">${String(minutes).padStart(2, '0')}:00</div><div class="modal-actions"><button class="button button-primary" id="timerStart">Comenzar</button><button class="button button-dark" id="timerReset">Reiniciar</button></div>${audioControls()}<p class="mini-note">Si algo duele, te marea o te falta el aire, detente. Cuidarte no es exigirte.</p>`; }
function crisisView() { return `<p class="eyebrow">Apoyo inmediato</p><h2 id="modalTitle">Vuelve a estar a salvo</h2><p class="modal-intro">Si sientes que podrías hacerte daño o no puedes mantenerte a salvo, busca apoyo humano ahora. No tienes que resolverlo solo/a.</p><div class="crisis-steps"><div><b>1</b><span>Aléjate de cualquier cosa con la que pudieras lastimarte.</span></div><div><b>2</b><span>Escribe o llama a alguien de confianza y dile: “Necesito compañía”.</span></div><div><b>3</b><span>Busca ayuda local de emergencia o una línea de apoyo.</span></div></div><a class="button button-primary crisis-link" href="https://findahelpline.com/es" target="_blank" rel="noreferrer">Encontrar ayuda local ↗</a><p class="mini-note">Esta app acompaña, pero no sustituye atención profesional.</p>`; }
function openModal(type, options = {}) { clearInterval(timer); clearInterval(phaseTimer); timer = phaseTimer = null; stopAudio(); const views = { breathing: breathingView(), movement: timerView('movement'), chairSquats: timerView('chairSquats'), walk: timerView('walk'), mobility: timerView('mobility'), balance: timerView('balance'), dance: timerView('dance'), crisis: crisisView(), social: `<p class="eyebrow">Social · 3 min</p><h2 id="modalTitle">Mensaje de conexión</h2><p class="modal-intro">Elige a alguien y envía este mensaje cuando te sientas listo/a.</p><div class="mini-note quote-message">“Hola, hoy pensé en ti. ¿Cómo estás realmente? No tienes que responder ahora, solo quería hacerte saber que me importas.”</div><button class="button button-primary" id="copyMessage">Copiar mensaje ↗</button><button class="button button-dark" id="doneSocial">Ya lo envié ✓</button><p class="modal-intro" id="copyStatus"></p>`, values: `<p class="eyebrow">Sentido · 2 min</p><h2 id="modalTitle">Brújula de valores</h2><p class="modal-intro">Nombra lo que importa y conviértelo en una acción pequeña.</p><label for="valueInput">Un valor importante para mí</label><input id="valueInput" placeholder="Ej. calma, honestidad, familia…" /><label for="actionInput">Hoy lo practicaré así</label><textarea id="actionInput" rows="3" placeholder="Una acción concreta y amable contigo…"></textarea><button class="button button-primary" id="saveValues">Guardar reflexión →</button><p class="modal-intro" id="valueStatus"></p>`, grounding: `<p class="eyebrow">Mental · 4 min</p><h2 id="modalTitle">Anclaje 5–4–3–2–1</h2><p class="modal-intro">Usa tus sentidos para regresar al aquí y ahora, sin prisa.</p><div class="sense-progress"><span id="senseCount">0</span>/5 sentidos explorados</div><div class="sense-list"><button data-sense="5 cosas que ves">5 · cosas que ves <b>＋</b></button><button data-sense="4 cosas que sientes">4 · cosas que sientes <b>＋</b></button><button data-sense="3 sonidos que oyes">3 · sonidos que oyes <b>＋</b></button><button data-sense="2 aromas que notas">2 · aromas que notas <b>＋</b></button><button data-sense="1 cosa que saboreas">1 · cosa que saboreas <b>＋</b></button></div><button class="button button-primary complete-sense" id="completeSense" disabled>Completar anclaje ✓</button><p class="modal-intro" id="senseStatus"></p>`, traffic: `<p class="eyebrow">Emocional · 3 min</p><h2 id="modalTitle">Semáforo emocional</h2><p class="modal-intro">Ubícate y elige un siguiente paso pequeño.</p><div class="traffic-options"><button data-traffic="rojo"><i></i><strong>Rojo</strong><small>Necesito parar y bajar la exigencia.</small></button><button data-traffic="amarillo"><i></i><strong>Amarillo</strong><small>Necesito observar y pedir claridad.</small></button><button data-traffic="verde"><i></i><strong>Verde</strong><small>Tengo energía para dar un paso.</small></button></div><textarea id="trafficNote" rows="2" placeholder="Ahora mismo necesito…"></textarea><button class="button button-primary" id="saveTraffic">Guardar mi siguiente paso →</button><p class="modal-intro" id="trafficStatus"></p>`, boundary: `<p class="eyebrow">Social · 3 min</p><h2 id="modalTitle">Practica un límite</h2><p class="modal-intro">Elige una situación y practica una frase clara.</p><div class="boundary-options"><button data-boundary="tiempo">No tengo tiempo</button><button data-boundary="energia">Necesito descansar</button><button data-boundary="opinion">Prefiero otra opción</button></div><div class="phrase-box" id="phraseBox">Elige una opción para ver una frase.</div><button class="button button-primary doneBoundary" id="doneBoundary" disabled>Practiqué mi frase ✓</button>`, gratitude: `<p class="eyebrow">Sentido · 4 min</p><h2 id="modalTitle">Gratitud 3 × 3</h2><p class="modal-intro">Escribe tres cosas pequeñas y por qué importan hoy.</p><div class="gratitude-grid"><input placeholder="Algo pequeño que agradezco…" /><input placeholder="Algo pequeño que agradezco…" /><input placeholder="Algo pequeño que agradezco…" /></div><textarea id="gratitudeWhy" rows="3" placeholder="¿Por qué estas tres cosas importan hoy?"></textarea><button class="button button-primary" id="saveGratitude">Guardar mi mirada →</button><p class="modal-intro" id="gratitudeStatus"></p>` }; modalContent.innerHTML = views[type] || views.breathing; modalBackdrop.hidden = false; document.body.style.overflow = 'hidden'; if (type === 'breathing') setupBreathing(options.breath); else if (timerViews[type]) setupTimer(type); bindModalActions(type); $('#modalClose').focus(); }
function setupAudioControls() { const toggle = $('#soundToggle'); const select = $('#audioMode'); const vibration = $('#vibrationToggle'); if (select) { select.value = audioMode; select.addEventListener('change', e => { audioMode = e.target.value; if (audio) { stopAudio(); startAudio(audioMode); toggle.textContent = '♩ Sonido suave'; toggle.setAttribute('aria-pressed', 'true'); } }); } toggle?.addEventListener('click', () => { if (audio) { stopAudio(); toggle.textContent = '♩ Sonido apagado'; toggle.setAttribute('aria-pressed', 'false'); } else if (startAudio(audioMode)) { toggle.textContent = '♩ Sonido suave'; toggle.setAttribute('aria-pressed', 'true'); } }); vibration?.addEventListener('click', () => { const enabled = !store.get('vibration', true); store.set('vibration', enabled); vibration.textContent = enabled ? '⌁ Vibración activa' : '⌁ Vibración apagada'; vibration.setAttribute('aria-pressed', String(enabled)); showToast(enabled ? 'Vibración activada.' : 'Vibración apagada.'); }); }
function setupBreathing(initialMode = '') { let mode = initialMode || 'calm', phase = 0, phaseRemaining = 0; const duration = 120; secondsLeft = duration; const update = () => { $('#timerDisplay').textContent = `${String(Math.floor(secondsLeft / 60)).padStart(2, '0')}:${String(secondsLeft % 60).padStart(2, '0')}`; }; const startButton = $('#timerStart'); update(); function setPhase() { const current = breathingModes[mode], seconds = current.pattern[phase]; phaseRemaining = seconds; const orb = $('#breathOrb'); orb.dataset.phase = phase === 0 ? 'in' : phase === current.pattern.length - 1 ? 'out' : 'hold'; $('#breathPhase').textContent = current.labels[phase]; $('#breathSeconds').textContent = `${seconds}s`; orb.style.setProperty('--phase-duration', `${seconds}s`); $('#breathHint').textContent = current.hint; chime(); } $$('.breathing-mode').forEach(btn => btn.addEventListener('click', () => { $$('.breathing-mode').forEach(b => b.classList.remove('selected')); btn.classList.add('selected'); mode = btn.dataset.breathMode; phase = 0; setPhase(); })); setupAudioControls(); startButton.addEventListener('click', () => { if (timer) return; if (startAudio(audioMode)) { $('#soundToggle').textContent = '♩ Sonido suave'; $('#soundToggle').setAttribute('aria-pressed', 'true'); } startButton.textContent = 'En curso…'; setPhase(); phaseTimer = setInterval(() => { phaseRemaining--; if (phaseRemaining <= 0) { phase = (phase + 1) % breathingModes[mode].pattern.length; setPhase(); } else $('#breathSeconds').textContent = `${phaseRemaining}s`; }, 1000); timer = setInterval(() => { secondsLeft--; update(); if (secondsLeft <= 0) { clearInterval(timer); clearInterval(phaseTimer); timer = phaseTimer = null; $('#timerLabel').textContent = 'Práctica completada · vuelve lentamente'; startButton.textContent = 'Completado ✓'; markToday('Respiración completada'); } }, 1000); }); $('#timerReset').addEventListener('click', () => { clearInterval(timer); clearInterval(phaseTimer); timer = phaseTimer = null; stopAudio(); secondsLeft = duration; phase = 0; update(); $('#timerLabel').textContent = '2 min · a tu propio ritmo'; startButton.textContent = 'Comenzar'; $('#soundToggle').textContent = '♩ Sonido apagado'; $('#soundToggle').setAttribute('aria-pressed', 'false'); setPhase(); }); $(`[data-breath-mode="${mode}"]`)?.classList.add('selected'); setPhase(); }
function setupTimer(type) { const durations = { movement: 300, chairSquats: 360, walk: 480, mobility: 300, balance: 240, dance: 420 }; secondsLeft = durations[type]; const update = () => { $('#timerDisplay').textContent = `${String(Math.floor(secondsLeft / 60)).padStart(2, '0')}:${String(secondsLeft % 60).padStart(2, '0')}`; }; const startButton = $('#timerStart'); update(); setupAudioControls(); startButton.addEventListener('click', () => { if (timer) return; if (startAudio(audioMode)) $('#soundToggle').textContent = '♩ Sonido suave'; startButton.textContent = 'En curso…'; const cues = ['Siente tus pies', 'Suaviza hombros', 'Respira', 'Sigue a tu ritmo']; let cue = 0; $('#movementCue').textContent = cues[cue]; phaseTimer = setInterval(() => { cue = (cue + 1) % cues.length; $('#movementCue').textContent = cues[cue]; chime(); }, 30000); timer = setInterval(() => { secondsLeft--; update(); if (secondsLeft <= 0) { clearInterval(timer); clearInterval(phaseTimer); timer = phaseTimer = null; $('#timerLabel').textContent = 'Práctica completada'; startButton.textContent = 'Completado ✓'; markToday('Movimiento completado'); } }, 1000); }); $('#timerReset').addEventListener('click', () => { clearInterval(timer); clearInterval(phaseTimer); timer = phaseTimer = null; stopAudio(); secondsLeft = durations[type]; update(); startButton.textContent = 'Comenzar'; $('#soundToggle').textContent = '♩ Sonido apagado'; $('#soundToggle').setAttribute('aria-pressed', 'false'); $('#movementCue').textContent = 'Listo para moverte'; }); }
function bindModalActions(type) { if (type === 'social') { $('#copyMessage').addEventListener('click', async () => { try { await navigator.clipboard.writeText($('.quote-message').textContent.replace(/[“”]/g, '')); $('#copyStatus').textContent = 'Mensaje copiado. Envíalo cuando te sientas listo/a.'; } catch { $('#copyStatus').textContent = 'Selecciona y copia el mensaje desde el recuadro.'; } }); $('#doneSocial').addEventListener('click', () => { markToday('Conexión registrada'); closeModal(); }); } if (type === 'values') $('#saveValues').addEventListener('click', () => { if (!$('#valueInput').value.trim() || !$('#actionInput').value.trim()) { $('#valueStatus').textContent = 'Completa ambos campos para guardar.'; return; } store.set('lastReflection', { value: $('#valueInput').value, action: $('#actionInput').value, date: new Date().toLocaleDateString('es-MX') }); $('#valueStatus').textContent = 'Guardado en este dispositivo.'; markToday('Reflexión guardada'); }); if (type === 'grounding') { $$('.sense-list button').forEach(btn => btn.addEventListener('click', () => { btn.classList.toggle('checked'); btn.querySelector('b').textContent = btn.classList.contains('checked') ? '✓' : '＋'; const count = $$('.sense-list button.checked').length; $('#senseCount').textContent = count; $('#completeSense').disabled = count < 5; })); $('#completeSense').addEventListener('click', () => { markToday('Anclaje completado'); $('#senseStatus').textContent = 'Volviste a tus sentidos. Bien hecho.'; }); } if (type === 'traffic') { $$('.traffic-options button').forEach(btn => btn.addEventListener('click', () => { $$('.traffic-options button').forEach(b => b.classList.remove('selected')); btn.classList.add('selected'); })); $('#saveTraffic').addEventListener('click', () => { if (!$('.traffic-options button.selected')) { $('#trafficStatus').textContent = 'Elige un color primero.'; return; } markToday('Paso emocional guardado'); $('#trafficStatus').textContent = 'Guardado. Solo necesitas el siguiente paso.'; }); } if (type === 'boundary') { const phrases = { tiempo: '“Ahora no puedo comprometerme con eso. Te digo cuándo sí tengo espacio.”', energia: '“Hoy necesito descansar. Gracias por entenderlo.”', opinion: '“Prefiero otra opción. Esto es lo que me funciona mejor.”' }; $$('.boundary-options button').forEach(btn => btn.addEventListener('click', () => { $$('.boundary-options button').forEach(b => b.classList.remove('selected')); btn.classList.add('selected'); $('#phraseBox').textContent = phrases[btn.dataset.boundary]; $('#doneBoundary').disabled = false; })); $('#doneBoundary').addEventListener('click', () => { markToday('Límite practicado'); closeModal(); }); } if (type === 'gratitude') $('#saveGratitude').addEventListener('click', () => { if ($$('.gratitude-grid input').some(input => !input.value.trim()) || !$('#gratitudeWhy').value.trim()) { $('#gratitudeStatus').textContent = 'Completa las tres cosas y el motivo.'; return; } markToday('Gratitud guardada'); $('#gratitudeStatus').textContent = 'Guardado. Gracias por mirar lo pequeño.'; }); }
function filterCards(filter = 'all', intent = '') { $$('.exercise-card').forEach(card => { const ok = (filter === 'all' || card.dataset.category === filter) && (!intent || card.dataset.intents.includes(intent)); card.classList.toggle('hidden-card', !ok); }); }
function renderWeek() { const names = ['Lun', 'Mar', 'Mié', 'Jue', 'Vie', 'Sáb', 'Dom'], today = new Date(), monday = new Date(today); monday.setDate(today.getDate() - ((today.getDay() + 6) % 7)); const progress = store.get('weekProgress', {}); $('#weekDays').innerHTML = names.map((name, i) => { const date = new Date(monday); date.setDate(monday.getDate() + i); const key = date.toISOString().slice(0, 10); const isToday = key === todayKey(); return `<div class="day ${isToday ? 'today' : ''}"><span>${name}</span><button aria-label="${name}, ${progress[key] ? 'completado' : 'sin completar'}" class="${progress[key] ? 'done' : ''}" data-day="${key}">${progress[key] ? '✓' : ''}</button></div>`; }).join(''); $$('.day button').forEach(button => button.addEventListener('click', () => { const p = store.get('weekProgress', {}); p[button.dataset.day] ? delete p[button.dataset.day] : p[button.dataset.day] = true; store.set('weekProgress', p); renderWeek(); refreshProgress(); })); }
function refreshProgress() { const progress = store.get('weekProgress', {}), now = Date.now(), count = Object.keys(progress).filter(key => { const age = (now - new Date(`${key}T00:00:00`).getTime()) / 86400000; return age >= -1 && age < 7; }).length; $('#progressCount').textContent = count; $('#progressBar').style.width = `${Math.min(count / 7 * 100, 100)}%`; $('#progressMessage').textContent = count === 0 ? 'Puedes empezar cuando te venga bien. No hay prisa.' : count === 1 ? 'Un día a la vez. Ya comenzaste.' : `${count} momentos de cuidado propio esta semana.`; }
function updateRecommendation(mood = store.get('moodToday', {})?.mood || 3) { const rec = recommendations[mood] || recommendations[3]; $('#recommendationLabel').textContent = rec.label; $('#recommendationText').textContent = rec.text; $('#recommendationButton').dataset.modal = rec.mode; $('#recommendationButton').dataset.breath = rec.breath || ''; $('#moodRoute').textContent = `Basado en tu check-in · ${new Date().toLocaleTimeString('es-MX', { hour: '2-digit', minute: '2-digit' })}`; }
$$('.open-exercise').forEach(button => button.addEventListener('click', () => openModal(button.dataset.modal)));
$('#modalClose').addEventListener('click', closeModal); modalBackdrop.addEventListener('click', e => { if (e.target === modalBackdrop) closeModal(); }); document.addEventListener('keydown', e => { if (e.key === 'Escape' && !modalBackdrop.hidden) closeModal(); });
$$('.mood-options button').forEach(button => button.addEventListener('click', () => { $$('.mood-options button').forEach(b => b.classList.remove('selected')); button.classList.add('selected'); const mood = button.dataset.mood; store.set('moodToday', { mood, date: new Date().toDateString() }); $('#moodMessage').textContent = 'Guardado. Tu recomendación se actualizará abajo.'; updateRecommendation(mood); showToast('Check-in guardado'); })); const moodSaved = store.get('moodToday', null); if (moodSaved?.date === new Date().toDateString()) $(`[data-mood="${moodSaved.mood}"]`)?.classList.add('selected');
$$('.category-tabs button').forEach(button => button.addEventListener('click', () => { $$('.category-tabs button').forEach(b => b.classList.remove('active')); button.classList.add('active'); $$('.intent-card').forEach(b => b.classList.remove('active')); filterCards(button.dataset.filter); }));
$$('.intent-card').forEach(button => button.addEventListener('click', () => { $$('.intent-card').forEach(b => b.classList.remove('active')); button.classList.add('active'); filterCards('all', button.dataset.filterIntent); $('#biblioteca').scrollIntoView({ behavior: 'smooth' }); }));
$('#clearSearch').addEventListener('click', () => { $('#exerciseSearch').value = ''; filterCards(); }); $('#exerciseSearch').addEventListener('input', e => { $$('.exercise-card').forEach(card => card.classList.toggle('hidden-card', !card.textContent.toLowerCase().includes(e.target.value.toLowerCase()))); });
$('#surpriseBtn').addEventListener('click', () => { const cards = $$('.exercise-card:not(.hidden-card)'); const card = cards[Math.floor(Math.random() * cards.length)]; if (card) openModal(card.querySelector('.open-exercise').dataset.modal); });
$('#recommendationButton').addEventListener('click', e => openModal(e.currentTarget.dataset.modal, { breath: e.currentTarget.dataset.breath })); $('#quickSessionButton').addEventListener('click', () => openModal('breathing', { breath: 'calm' })); $('#crisisButton').addEventListener('click', () => openModal('crisis'));
$('#accessibilityToggle').addEventListener('click', () => { document.body.classList.toggle('large-text'); store.set('largeText', document.body.classList.contains('large-text')); }); $('#motionToggle').addEventListener('click', () => { const reduced = !document.body.classList.contains('reduced-motion'); document.body.classList.toggle('reduced-motion', reduced); store.set('reducedMotion', reduced); $('#motionToggle').textContent = reduced ? 'Animación: reducida' : 'Animación: activa'; });
const savedJournal = store.get('journal', null); if (savedJournal) { $('#journalInput').value = savedJournal.text; $('#savedJournal').innerHTML = `<strong>Última nota · ${savedJournal.date}</strong><br>${savedJournal.text}`; } $('#saveJournal').addEventListener('click', () => { const text = $('#journalInput').value.trim(); if (!text) { $('#journalStatus').textContent = 'Escribe algo antes de guardar.'; return; } const entry = { text, date: new Date().toLocaleDateString('es-MX') }; store.set('journal', entry); $('#savedJournal').innerHTML = `<strong>Última nota · ${entry.date}</strong><br>${entry.text}`; $('#journalStatus').textContent = 'Guardado localmente en este dispositivo.'; markToday('Nota guardada'); });
$('#refreshQuestion').addEventListener('click', () => { const current = $('#dailyQuestion').textContent; const available = questions.filter(q => q !== current); $('#dailyQuestion').textContent = available[Math.floor(Math.random() * available.length)]; }); $('#themeToggle').addEventListener('click', () => { document.body.classList.toggle('dark'); const dark = document.body.classList.contains('dark'); store.set('darkMode', dark); $('#themeToggle').textContent = dark ? '☾' : '☼'; }); if (store.get('darkMode', false)) { document.body.classList.add('dark'); $('#themeToggle').textContent = '☾'; } if (store.get('largeText', false)) document.body.classList.add('large-text'); if (store.get('reducedMotion', false)) { document.body.classList.add('reduced-motion'); $('#motionToggle').textContent = 'Animación: reducida'; }
const hour = new Date().getHours(); $('#timeGreeting').textContent = hour < 12 ? 'Buenos días' : hour < 19 ? 'Buenas tardes' : 'Buenas noches'; $('#timeHint').textContent = hour >= 20 ? 'Modo noche: menos estímulos, más espacio para descansar.' : hour < 12 ? 'Un comienzo amable para tu día.' : 'Una pausa breve también cuenta.';
$('#profileBtn').addEventListener('click', () => showToast('Tu espacio es privado: los datos viven en este navegador.')); renderWeek(); refreshProgress(); updateRecommendation(); if ('serviceWorker' in navigator) window.addEventListener('load', () => navigator.serviceWorker.register('./sw.js').catch(() => {})); window.addEventListener('beforeunload', stopAudio);
